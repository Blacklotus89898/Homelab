apiVersion: v1
kind: Service
metadata:
  name: audiobookshelf-svc
  namespace: audiobookshelf
  labels:
    app: audiobookshelf
spec:
  type: NodePort
  selector:
    app: audiobookshelf
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030
      protocol: TCP
