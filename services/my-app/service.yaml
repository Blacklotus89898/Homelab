apiVersion: v1
kind: Service
metadata:
  name: my-app-svc
  namespace: test
  labels:
    app: my-app
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000
      protocol: TCP
