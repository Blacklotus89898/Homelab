apiVersion: v1
kind: Service
metadata:
  name: kavita-service
  namespace: kavita
  labels:
    app: kavita
spec:
  type: NodePort
  selector:
    app: kavita
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30050
      protocol: TCP
