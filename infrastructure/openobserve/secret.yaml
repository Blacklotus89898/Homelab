# TODO: seal this with kubeseal before pushing to a shared/public repo
# kubectl create secret generic openobserve-auth -n openobserve \
#   --from-literal=email=admin@homelab.local \
#   --from-literal=password=YOURPASSWORD \
#   --dry-run=client -o yaml | kubeseal -o yaml > infrastructure/openobserve/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: openobserve-auth
  namespace: openobserve
stringData:
  email: "admin@homelab.local"
  password: "CHANGE_ME"
