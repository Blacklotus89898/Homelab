apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Gateway API standard channel — required by Istio ambient mode
  # Check latest: https://github.com/kubernetes-sigs/gateway-api/releases
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.1/standard-install.yaml
